import 'package:flutter/material.dart';

class GithubAnalyzerScreen extends StatelessWidget {
  const GithubAnalyzerScreen({super.key});

  @override 
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("GitHub Analyzer")),
      body: const Center(
        child: Text("GitHub Analyzer Coming Soon"),
      ),
    );
  }
}